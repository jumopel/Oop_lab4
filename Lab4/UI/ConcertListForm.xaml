<Window x:Class="Lab4.UI.ConcertListForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Height="400" Width="600" WindowStartupLocation="CenterScreen" Closing="Window_Closing">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Список усіх запланованих концертів" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <ListBox x:Name="lstConcerts" Grid.Row="1" SelectionChanged="lstConcerts_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding Organizer}" FontSize="16" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Foreground="Gray" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Performances.Count, StringFormat='Виступів: {0}'}" Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Створити концерт" Width="130" Height="30" Margin="5" Click="btnAddConcert_Click"/>
            <Button x:Name="btnEditConcert" Content="Редагувати деталі" Width="130" Height="30" Margin="5" Click="btnEditConcert_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>